var factory = require('../sampleFactory')
/*
describe('blocks ', function(){

    beforeEach(function(){
        this.block = factory.get()[0]
    })

    describe('getters ', function(){

        describe('getInputs', function(){
            beforeEach(function(){
                this.i = this.block.getInputs();
            })
            it('should return inputs',function(){
                expect( this.i.length ).toBe(1);
                expect( this.i[0].x ).toBe(7);
                expect( this.i[0].y ).toBe(5);
            })
        })

        describe('getOutputs', function(){
            beforeEach(function(){
                this.o = this.block.getOutputs();
            })
            it('should return outputs',function(){
                expect( this.o.length ).toBe(1);
                expect( this.o[0].x ).toBe(7);
                expect( this.o[0].y ).toBe(8);
            })
        })

    })

})
*/
